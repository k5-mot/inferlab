defaults:
  interval: 24h
  concurrency: 1

sources:
  - name: nextcloud-documents
    source: "/nextcloud/data/k5-mot/files/Documents"
    kb-id: "${NEXTCLOUD_OPENWEBUI_KB_ID:-}"
    filter:
      exclude:
        - "**/.DS_Store"
        - "**/Thumbs.db"
        - "**/*.url"
