use_default_settings: true

general:
  open_metrics: "sx_metrics_password"

server:
  # ComposeのSEARXNG_SECRETがこの非機密placeholderを上書きする。
  secret_key: "overridden-by-SEARXNG_SECRET"
  limiter: false

search:
  formats:
    - html
    - json
